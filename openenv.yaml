name: email-triage-env-v2
version: "2.0.0"
description: >
  Sequential email triage with thread dependencies, SLA clocks,
  phishing detection, escalation budgets, and context-sensitive grading.
tasks:
  - id: classify_urgency
    difficulty: easy
    max_steps: 5
  - id: triage_and_route
    difficulty: medium
    max_steps: 20
  - id: inbox_zero
    difficulty: hard
    max_steps: 50
